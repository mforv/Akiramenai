<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mforv.progs.japan"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="14"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".JapTutorActivity"
            android:label="@string/app_name" android:theme="@android:style/Theme.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Tutoring" android:label="@string/tutoringheadline" android:theme="@android:style/Theme.Light" android:configChanges="orientation"></activity>
        <activity android:name="TestBody" android:label="@string/testbodyheadline" android:theme="@android:style/Theme.Light" android:configChanges="orientation"></activity>
        <activity android:name="TestPrep" android:label="@string/testprepheadline" android:theme="@android:style/Theme.Light"></activity>
        <activity android:theme="@android:style/Theme.Light" android:label="@string/tutoringheadline" android:name="ContentsActivity"></activity>
        <activity android:theme="@android:style/Theme.Light" android:label="@string/statisticsheadline" android:name="StatisticsActivity"></activity>
        <activity android:label="@string/settingsheadline" android:name="PreferencesActivtity" android:theme="@android:style/Theme.Light"></activity>
        <activity android:name="AboutActivity" android:label="@string/about_headline"></activity>
    </application>

</manifest>